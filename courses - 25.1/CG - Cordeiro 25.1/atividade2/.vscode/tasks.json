{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compilar OpenGL",
            "type": "shell",
            "command": "gcc",
            "args": [
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "-lglut", "-lGL", "-lGLU", "-lm"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}